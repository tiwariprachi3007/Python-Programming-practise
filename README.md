
   # 🐍 Python Programming Practice

This repository is my first step into Python programming, organized into folders and files that demonstrate fundamental concepts like variables, data types, expressions, conditionals, loops, functions, recursion, file handling, lists, tuples, dictionaries, and sets.

---

## 📂 Folder Structure

### 1. Variables and Data Types
- `avg.py` → Calculate average of numbers  
- `difference.py` → Find difference between two values  
- `exp_execution.py` → Demonstrate expression execution  
- `first_program.py` → Simple "Hello World" starter program  
- `operators.py` → Show usage of arithmetic and logical operators  
- `square_area.py` → Compute area of a square  
- `sum.py` → Add two numbers  

---

### 2. Strings & Conditional Statements
- `concatenate.py` → Join two or more strings  
- `grade.py` → Assign grade based on marks  
- `greatest.py` → Find the greatest among numbers  
- `len.py` → Calculate length of a string  
- `multiple.py` → Check if a number is a multiple of another  
- `oddeven.py` → Determine odd/even numbers  

---

### 3. Dictionary & Sets
- `dictioanary_methods.py` → Explore dictionary methods  
- `set.py` → Basic set creation and usage  
- `set_method.py` → Demonstrate set operations (union, intersection, difference)  

---

### 4. Loops
- `break.py` → Demonstrate breaking out of a loop  
- `continue.py` → Skip current iteration  
- `factorial.py` → Calculate factorial using loops  
- `forloop.py` → Basic usage of `for` loop  
- `list_print.py` → Print elements of a list  
- `no_print.py` → Example of loop with no output condition  
- `pass.py` → Demonstrate the `pass` statement  
- `range.py` → Use of `range()` in loops  
- `search.py` → Search for an element in a list  
- `sum.py` → Find sum of numbers using loop  
- `table.py` → Print multiplication table  

---

### 5. Function & Recursion
- `average.py` → Calculate average using a function  
- `factorial.py` → Find factorial using recursion  
- `len.py` → Implement length calculation with a function  
- `sum.py` → Compute sum of numbers using a function  

---

### 6. File Input/Output
- `creatingfile.py` → Create a new file and write content  
- `function.py` → File operations via functions  
- `search.py` → Search for specific content inside a file  

---

### 7. Lists & Tuples
- `count_tuple.py` → Count elements inside a tuple  
- `list_method.py` → Explore list methods (append, remove, sort, etc.)  
- `palindrome.py` → Check palindrome using lists  
- `store_list.py` → Store and display elements in a list  
- `tuple.py` → Basic tuple creation and usage  
- `tuple_method.py` → Demonstrate tuple methods and operations  

---

## 🎯 Purpose
- Practice Python fundamentals step by step  
- Build confidence with small, modular scripts  
- Prepare for academic assignments and technical interviews  

---

## 🚀 How to Run
1. Clone or download this repository:
   ```bash
   git clone https://github.com/tiwariprachi3007/Python-Programming-practise.git
   cd Python-Programming-practise

